// Here is the sample website: https://codepen.io/ScottFSchmidt/pen/yLaoBQw
// Python verision should only be used because it is insanely easy to maniplate the data:
// https://github.com/ScottFrederickSchmidt/AccountingAutomation/blob/main/Subtotal%20Analyzer%20(Python)

function allPermutations(items) {
  let results = [];
  function permute(arr, memo) {
    var cur, memo = memo || [];
    for (let i = 0; i < arr.length; i++) {
      cur = arr.splice(i, 1);
      if (arr.length === 0) {
        results.push(memo.concat(cur));
      }
      permute(arr.slice(), memo.concat(cur));
      arr.splice(i, 0, cur[0]);
    }
    return results;
  }
  permute(items);
  return results;
}

function createArray() {
var numArray=[]

var num1=parseFloat(document.getElementById("num1").value);
var num2=parseFloat(document.getElementById("num2").value);
var num3=parseFloat(document.getElementById("num3").value);
var num4=parseFloat(document.getElementById("num4").value);
var num5=parseFloat(document.getElementById("num5").value);
var num6=parseFloat(document.getElementById("num6").value);
var num7=parseFloat(document.getElementById("num7").value);
var findNum=parseFloat(document.getElementById("findNum").value);
var values=[num1, num2, num3, num4, num5, num6, num7];

for(var i=0; i<values.length;i++ )
     { 
        if(values[i]!=0)
            numArray.push(values[i]); 
      } 
//console.log("num array of values is: ", numArray);

// var permutated = allPermutations(numArray);
// console.log(permutated);  
var answers=[] 
var combo=getCombinations(numArray);

function getCombinations(array) {

    function fork(i, t) {
        if (i === array.length) {
            result.push(t);
            return;
        }
        fork(i + 1, t.concat([array[i]]));
        fork(i + 1, t);
    }

    var result = [];
    fork(0, []);
    return result;
}

var data = numArray,
    result = getCombinations(data);
	
console.log(result); 
  
for (i=0; i<result.length; i++) {
  var sum = result[i].reduce((a, b) => a + b, 0);
  if (sum==findNum) {
    answers.push(" { "+ result[i]+ " }");
    console.log(result[i], " is equal to sum: ", sum);
// document.getElementById("displaySum").innerHTML = "Found sum: ", sum;
  }
} 
document.getElementById("permutation").innerHTML = answers;
}
//console.log("Finished.");

// Restricts input for the given textbox to the given inputFilter.
function setInputFilter(textbox, inputFilter) {
  ["input", "keydown", "keyup", "mousedown", "mouseup", "select", "contextmenu", "drop"].forEach(function(event) {
    textbox.addEventListener(event, function() {
      if (inputFilter(this.value)) {
        this.oldValue = this.value;
        this.oldSelectionStart = this.selectionStart;
        this.oldSelectionEnd = this.selectionEnd;
      } else if (this.hasOwnProperty("oldValue")) {
        this.value = this.oldValue;
        this.setSelectionRange(this.oldSelectionStart, this.oldSelectionEnd);
      } else {
        this.value = "";
      }
    });
  });
}


// Install input filters.
setInputFilter(document.getElementByClassName("currencyTextBox"), function(value) {
  return /^-?\d*[.,]?\d{0,2}$/.test(value); });
